<!DOCTYPE html>
<html>
  <head>
    <title>Welcome!</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1>Cards of the Day!</h1>
    <p>Hello these are the cards for today!</p>
    <% cards.forEach(function(c){ %>
    <!-- <tr> -->
    <!-- <td><%= c.id %></td>
      <td><%= c.name %></td>
      <td><%= c.atk %></td>
      <td><%= c.def %></td>
      <td><%= c.type %></td>
      <td><%= c.level %></td> -->
    <!-- <td> -->
    <form method="GET" action="/save/<%= c._id %>">
      <div id="image">
        <img src="<%= c.image %>" />
        <button type="submit">Save to your Collection</button>
      </div>
    </form>

    <!-- </td>
    </tr> -->
    <% }); %>
  </body>
</html>
